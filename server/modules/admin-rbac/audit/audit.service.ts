import { AppError } from "../../../common/middleware/error-handler";
import { findAll, findById, type AuditLog, type AuditLogWithUser } from "./audit.repository";
export type AuditData = { id: string; userId: string | null; userName?: string | null; userEmail?: string | null; action: string; resource: string; metadata: Record<string, unknown>; ipAddress: string; createdAt: string };
const toData = (log: AuditLogWithUser): AuditData => ({ id: log.id, userId: log.userId, userName: log.userName || undefined, userEmail: log.userEmail || undefined, action: log.action, resource: log.resource, metadata: log.metadata, ipAddress: log.ipAddress, createdAt: log.createdAt.toISOString() });
export const listAuditLogs = async (params: { limit: number; offset: number }): Promise<AuditData[]> => (await findAll(params)).map(toData);
export const getAuditLogById = async (id: string): Promise<AuditData> => { const log = await findById(id); if (!log) throw new AppError("Audit log not found", 404); return toData({ ...log, userName: undefined, userEmail: undefined }); };